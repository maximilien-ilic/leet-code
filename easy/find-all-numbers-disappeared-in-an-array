#Utilizing hash sets for existence checks versus in-place index manipulation to optimize space complexity.



class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        list = set(nums)
        result = []
        for i in range(1, len(nums)+1):
            if i not in list :
                result.append(i)  
        return result
