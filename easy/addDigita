class Solution:
    def addDigits(self, num: int) -> int:
        result = 0
        while num > 0:
            result += num % 10
            print(num % 10)
            num = num // 10
        if result > 9 :
            return self.addDigits(result)
        else:
         return result 